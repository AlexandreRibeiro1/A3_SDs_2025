# Back-End - Sistema de Reserva de Mesas

Servidor construído em Python com Flask (ou FastAPI) que gerencia as reservas de mesas.

## Requisitos

- Python 3.10+
- Flask
- SQLite (embutido)

## Instalação

```bash
pip install -r requirements.txt
